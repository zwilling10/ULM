<Window x:Class="ULM.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Universal Linux Manager"
        Width="1160" Height="750" MinWidth="900" MinHeight="580"
        Background="{DynamicResource BrushBg}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- ── Vorlage für eine einzelne Distro-Zeile (mit Spalten) ────── -->
        <DataTemplate x:Key="EntryTemplate">
            <Border Padding="0,5"
                    BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,0,0,1"
                    Background="Transparent"
                    MouseLeftButtonDown="EntryRow_MouseLeftButtonDown">
                <!--
                    Kein Tooltip auf dem Border (= gesamte Zeile) mehr.
                    Der Tooltip erscheint jetzt ausschließlich wenn der
                    Mauszeiger über dem Distro-Namen (Spalte 1) steht.
                -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="36"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="388"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>

                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}"
                              VerticalAlignment="Center" HorizontalAlignment="Center"/>

                    <!--
                        Hash-Status-Symbol: selbst gezeichneter Smiley (KEIN Emoji — dessen Eigenfarbe
                        lässt sich nicht ändern), komplett in EINER Farbe (Umriss, Augen, Mund) je nach
                        HashStatusBrush. Unsichtbar, solange kein Referenz-Hash vorliegt (ISO noch nie
                        heruntergeladen/importiert) — sonst würde jede unberührte ISO wie ein Problem
                        aussehen, siehe HasHashStatus/HashStatusBrush-Kommentar in IsoEntryViewModel.
                    -->
                    <Canvas Grid.Column="1" Width="16" Height="16"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding HasHashStatus, Converter={StaticResource BoolToVis}}"
                            ToolTip="{Binding HashStatusTooltip}">
                        <Ellipse Width="14" Height="14" Canvas.Left="1" Canvas.Top="1"
                                 Stroke="{Binding HashStatusBrush}" StrokeThickness="1.4" Fill="Transparent"/>
                        <Ellipse Width="1.8" Height="1.8" Canvas.Left="5.2" Canvas.Top="6"
                                 Fill="{Binding HashStatusBrush}"/>
                        <Ellipse Width="1.8" Height="1.8" Canvas.Left="9" Canvas.Top="6"
                                 Fill="{Binding HashStatusBrush}"/>
                        <Path Stroke="{Binding HashStatusBrush}" StrokeThickness="1.3"
                              Data="M 4.5,9.3 Q 8,12 11.5,9.3"/>
                    </Canvas>

                    <!--
                        Tooltip NUR auf dem Namens-TextBlock — nicht auf der
                        gesamten Zeile. ToolTipService.IsEnabled wird über
                        den ChkShowInfo-Schalter in der Statusleiste gesteuert.
                    -->
                    <TextBlock Grid.Column="2" Text="{Binding Name}"
                               Foreground="{Binding ForegroundBrush}"
                               FontSize="12.5" FontWeight="SemiBold"
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                               ToolTip="{Binding TipTooltip}"
                               ToolTipService.ShowDuration="20000"
                               ToolTipService.InitialShowDelay="400">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=ChkShowInfo}"
                                                 Value="False">
                                        <Setter Property="ToolTipService.IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock Grid.Column="3" Text="{Binding LocalStatus}"
                               Foreground="{DynamicResource BrushMid}"
                               FontSize="11.5" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="4" Text="{Binding UsbStatus}"
                               Foreground="{DynamicResource BrushMid}"
                               FontSize="11.5" VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="5" Text="{Binding VersionStatus}"
                               Foreground="{DynamicResource BrushMid}"
                               FontSize="11.5" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- ── Vorlage für eine Kategorie-Gruppe mit Sammel-Checkbox ───── -->
        <DataTemplate x:Key="CategoryTemplate">
            <StackPanel Margin="0,0,0,2">
                <Border Background="{DynamicResource BrushCard}" Padding="14,8" Margin="0,6,0,0">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding AllSelected, Mode=TwoWay}"
                                  IsThreeState="True"
                                  VerticalAlignment="Center" Margin="0,0,10,0"
                                  ToolTip="Alle Distros dieser Kategorie an-/abwählen"/>
                        <TextBlock Text="{Binding CategoryLabel}"
                                   FontWeight="Bold" FontSize="13.5"
                                   Foreground="{DynamicResource BrushHeader}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <ItemsControl ItemsSource="{Binding Entries}"
                              ItemTemplate="{StaticResource EntryTemplate}"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <!-- ── Header ─────────────────────────────────────────────────── -->
        <!-- BrushHeaderBar (nicht BrushHeader!): dieser Streifen soll in JEDEM Theme dunkel
             bleiben, nicht mit dem Farbschema invertieren — siehe Kommentar in AppTheme.xaml. -->
        <Border Grid.Row="0" Background="{DynamicResource BrushHeaderBar}">
            <Grid Margin="18,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="40" Height="40" CornerRadius="8"
                            Background="{DynamicResource BrushBlue}" Margin="0,0,12,0">
                        <TextBlock Text="UL" Foreground="White" FontWeight="Bold"
                                   FontSize="14" HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Universal Linux Manager"
                                   Foreground="White" FontSize="16" FontWeight="Bold"
                                   FontFamily="{DynamicResource FontMain}"/>
                        <TextBlock Text="USB-Stick einrichten · Linux ISOs verwalten · Downloads überwachen"
                                   Foreground="{DynamicResource BrushDim}" FontSize="10"
                                   FontFamily="{DynamicResource FontMain}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Startphasen-Hinweis: rotierender Spinner + pulsierender Text, sichtbar solange
                     Online-Versionscheck ODER Stick-Scan läuft — hält Anwender/Experte vom
                     vorschnellen Klicken ab, bis Datenbank- und Stick-Stand vollständig sind. -->
                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding ScanInProgress, Converter={StaticResource BoolToVis}}">
                    <Ellipse Width="15" Height="15" Stroke="#FFC107" StrokeThickness="2.3"
                             StrokeDashArray="3.4 2.2" StrokeDashCap="Round"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="ScanSpinnerRotate" Angle="0"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ScanSpinnerRotate"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0" To="360" Duration="0:0:1"
                                                         RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                    <TextBlock Margin="8,0,0,0" VerticalAlignment="Center"
                               Foreground="#FFC107" FontSize="10.5" FontWeight="SemiBold"
                               Text="{Binding ScanHintText}">
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="1.0" To="0.35" Duration="0:0:0.85"
                                                         AutoReverse="True" RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal"
                            VerticalAlignment="Center" HorizontalAlignment="Right">

                    <Border Visibility="{Binding OnlineScanActive, Converter={StaticResource BoolToVis}}"
                            Background="#0D2035" CornerRadius="5"
                            Padding="10,6" Margin="0,0,10,0" MinWidth="140">
                        <StackPanel>
                            <DockPanel LastChildFill="False">
                                <TextBlock DockPanel.Dock="Left" Text="🌐 Online-Scan"
                                           Foreground="{DynamicResource BrushDim}"
                                           FontSize="9" FontWeight="SemiBold"/>
                                <TextBlock DockPanel.Dock="Right"
                                           Text="{Binding OnlineScanPercent, Mode=OneWay, StringFormat={}{0}%}"
                                           Foreground="White" FontSize="9" HorizontalAlignment="Right"/>
                            </DockPanel>
                            <ProgressBar Value="{Binding OnlineScanPercent, Mode=OneWay}" Maximum="100"
                                         Height="5" Margin="0,4,0,0"
                                         Background="#1A3355" Foreground="#0075BE" BorderThickness="0"/>
                        </StackPanel>
                    </Border>

                    <Border Visibility="{Binding UsbScanActive, Converter={StaticResource BoolToVis}}"
                            Background="#0D2035" CornerRadius="5"
                            Padding="10,6" Margin="0,0,10,0" MinWidth="130">
                        <StackPanel>
                            <TextBlock Text="💾 Stick-Scan"
                                       Foreground="{DynamicResource BrushDim}"
                                       FontSize="9" FontWeight="SemiBold"/>
                            <ProgressBar IsIndeterminate="True" Height="5" Margin="0,4,0,0"
                                         Background="#1A3355" Foreground="#27AE60" BorderThickness="0"/>
                        </StackPanel>
                    </Border>

                    <Border Visibility="{Binding HealthCheckActive, Converter={StaticResource BoolToVis}}"
                            Background="#0D2035" CornerRadius="5"
                            Padding="10,6" Margin="0,0,16,0" MinWidth="140">
                        <StackPanel>
                            <DockPanel LastChildFill="False">
                                <TextBlock DockPanel.Dock="Left" Text="🩺 Gesundheitscheck"
                                           Foreground="{DynamicResource BrushDim}"
                                           FontSize="9" FontWeight="SemiBold"/>
                                <TextBlock DockPanel.Dock="Right"
                                           Text="{Binding HealthCheckPercent, Mode=OneWay, StringFormat={}{0}%}"
                                           Foreground="White" FontSize="9" HorizontalAlignment="Right"/>
                            </DockPanel>
                            <ProgressBar Value="{Binding HealthCheckPercent, Mode=OneWay}" Maximum="100"
                                         Height="5" Margin="0,4,0,0"
                                         Background="#1A3355" Foreground="#0075BE" BorderThickness="0"/>
                        </StackPanel>
                    </Border>

                    <Border Width="1" Height="28" Background="#2A4A6A" Margin="0,0,14,0">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding OnlineScanActive}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding UsbScanActive}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding HealthCheckActive}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                    </Border>

                    <Button x:Name="BtnModeToggle" Style="{DynamicResource BtnGhost}"
                            Foreground="White" BorderBrush="#4A6785"
                            Click="BtnModeToggle_Click" Width="160" Margin="0,0,8,0"/>

                    <Button x:Name="BtnThemeToggle" Style="{DynamicResource BtnGhost}"
                            Foreground="White" BorderBrush="#4A6785"
                            ToolTip="Erscheinungsbild umschalten: System / Hell / Dunkel"
                            Click="BtnThemeToggle_Click" Width="130" Margin="0,0,8,0"/>

                    <Button x:Name="BtnHelp" Content="❓ Hilfe"
                            Style="{DynamicResource BtnGhost}"
                            Foreground="White" BorderBrush="#4A6785"
                            Click="BtnHelp_Click" Width="80"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Toolbar: Laufwerk / Ventoy ─────────────────────────────── -->
        <Border Grid.Row="1" Background="{DynamicResource BrushWhite}"
                BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,0,0,1">
            <Grid Margin="18,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="36"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="ZIEL-USB-LAUFWERK"
                           FontSize="9" FontWeight="Bold" Foreground="{DynamicResource BrushDim}"
                           VerticalAlignment="Center" Margin="0,0,10,0"/>

                <ComboBox Grid.Column="1" x:Name="DriveCombo"
                          ItemsSource="{Binding Drives}"
                          SelectedItem="{Binding SelectedDrive, Mode=TwoWay}"
                          DisplayMemberPath="DisplayName"/>

                <Button Grid.Column="2" x:Name="BtnRefreshDrives"
                        Content="↻" Style="{DynamicResource BtnGhost}"
                        Width="32" Height="32" FontSize="16"
                        ToolTip="Laufwerke neu einlesen"
                        Click="BtnRefreshDrives_Click"/>

                <Button Grid.Column="3" x:Name="BtnVentoy"
                        Content="⚡ Ventoy installieren"
                        Style="{DynamicResource BtnSuccess}"
                        Margin="10,0,0,0" Click="BtnVentoy_Click"/>

                <CheckBox Grid.Column="4" x:Name="ChkSecureBoot"
                          Content="🔒 Secure Boot" Margin="14,0,0,0"
                          VerticalAlignment="Center"
                          Checked="ChkSecureBoot_Changed"
                          Unchecked="ChkSecureBoot_Changed"/>

                <TextBlock Grid.Column="6" x:Name="DriveInfoTxt"
                           Text="{Binding DriveInfoText}"
                           Foreground="{DynamicResource BrushBlue}"
                           FontWeight="SemiBold" FontSize="12"
                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- ── Haupt-Inhalt ────────────────────────────────────────────── -->
        <TabControl Grid.Row="2" x:Name="MainTabs" Margin="0">

            <TabItem Header="ISO-Auswahl">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource BrushCard}"
                            BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,0,0,1"
                            Padding="0,7">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36"/>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="388"/>
                                <ColumnDefinition Width="130"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="1" Text="🔒" FontSize="10"
                                       HorizontalAlignment="Center"
                                       ToolTip="Hash-Status: grün = Prüfsumme vorhanden, rot = Integritätsprüfung fehlgeschlagen"
                                       Foreground="{DynamicResource BrushMid}"/>
                            <TextBlock Grid.Column="2" Text="Linux-Distribution  (Haken = Download)"
                                       FontWeight="Bold" FontSize="11" Foreground="{DynamicResource BrushMid}"/>
                            <TextBlock Grid.Column="3" Text="Lokal"
                                       FontWeight="Bold" FontSize="11" Foreground="{DynamicResource BrushMid}"/>
                            <TextBlock Grid.Column="4" Text="Auf dem Stick"
                                       FontWeight="Bold" FontSize="11" Foreground="{DynamicResource BrushMid}"/>
                            <TextBlock Grid.Column="5" Text="Aktuell"
                                       FontWeight="Bold" FontSize="11" Foreground="{DynamicResource BrushMid}"/>
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                  Background="{DynamicResource BrushWhite}">
                        <ItemsControl ItemsSource="{Binding Categories}"
                                      ItemTemplate="{StaticResource CategoryTemplate}"
                                      Margin="4,2,4,12"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <TabItem Header="Protokoll" x:Name="LogTab">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBox Grid.Row="0" x:Name="LogBox"
                             IsReadOnly="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas, Courier New"
                             FontSize="11" BorderThickness="0"
                             Background="{DynamicResource BrushWhite}"/>
                    <Button Grid.Row="1" Content="Protokoll leeren"
                            Style="{DynamicResource BtnGhost}"
                            HorizontalAlignment="Right" Margin="8"
                            Click="BtnClearLog_Click"/>
                </Grid>
            </TabItem>

            <TabItem Header="Status" x:Name="StatusTab">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Background="{DynamicResource BrushWhite}">
                    <StackPanel Margin="12">

                        <TextBlock Text="Aktueller Vorgang" FontWeight="Bold" FontSize="13"
                                   Foreground="{DynamicResource BrushMid}" Margin="0,0,0,4"/>
                        <TextBlock Margin="0,0,0,12" FontSize="12">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="{Binding StatusText}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsBusy}" Value="False">
                                            <Setter Property="Text" Value="Kein manueller Vorgang aktiv."/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBlock Text="Automatische Hintergrund-Scans" FontWeight="Bold" FontSize="13"
                                   Foreground="{DynamicResource BrushMid}" Margin="0,0,0,4"/>
                        <TextBlock Margin="0,0,0,2" FontSize="12">
                            <Run Text="🌐 Online-Versionscheck: "/>
                            <Run Text="läuft …" Foreground="{DynamicResource BrushBlue}">
                                <Run.Style>
                                    <Style TargetType="Run">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding OnlineScanActive}" Value="False">
                                                <Setter Property="Text" Value="inaktiv"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Run.Style>
                            </Run>
                        </TextBlock>
                        <TextBlock Margin="0,0,0,12" FontSize="12">
                            <Run Text="💾 Stick-Prüfung: "/>
                            <Run Text="läuft …" Foreground="{DynamicResource BrushBlue}">
                                <Run.Style>
                                    <Style TargetType="Run">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding UsbScanActive}" Value="False">
                                                <Setter Property="Text" Value="inaktiv"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Run.Style>
                            </Run>
                        </TextBlock>

                        <TextBlock Text="Geplante automatische Aktionen" FontWeight="Bold" FontSize="13"
                                   Foreground="{DynamicResource BrushMid}" Margin="0,0,0,4"/>
                        <TextBlock Margin="0,0,0,2" FontSize="12">
                            <Run Text="🌐 Nächster automatischer Online-Versionscheck: "/>
                            <Run Text="{Binding NextAutoCheckText, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Margin="0,0,0,12" FontSize="12" Foreground="{DynamicResource BrushMid}"
                                   Text="🔌 Laufwerks-Überwachung: läuft laufend im Hintergrund (Prüfung alle 4 Sekunden)."/>

                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Verlauf" FontWeight="Bold" FontSize="13"
                                       Foreground="{DynamicResource BrushMid}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="Verlauf leeren" Style="{DynamicResource BtnGhost}"
                                    Click="BtnClearHistory_Click"/>
                        </Grid>
                        <ItemsControl ItemsSource="{Binding ActivityHistory}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontFamily="Consolas, Courier New" FontSize="11" Margin="0,1"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>

        </TabControl>

        <!-- ── Aktions-Leiste ──────────────────────────────────────────── -->
        <Border Grid.Row="3" Background="{DynamicResource BrushWhite}"
                BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,1,0,0">
            <Grid Margin="12,8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                    <Button x:Name="BtnDownload" Content="⬇  Herunterladen"
                            Style="{DynamicResource BtnPrimary}" Width="180"
                            Click="BtnDownload_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnUpdates" Content="↻  Updates prüfen"
                            Style="{DynamicResource BtnWarning}" Width="150"
                            Click="BtnUpdates_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnCancel" Content="✕  Stopp"
                            Style="{DynamicResource BtnDanger}" Width="100"
                            Visibility="Collapsed" Click="BtnCancel_Click" Margin="0,0,8,0"/>
                    <TextBlock x:Name="SpeedLbl" VerticalAlignment="Center"
                               Foreground="{DynamicResource BrushBlue}"
                               FontSize="11.5" Margin="8,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" x:Name="ExpertBar"
                            Orientation="Horizontal" Margin="0,0,0,6">
                    <Button x:Name="BtnCheckUrls" Content="🌐  URLs prüfen"
                            Style="{DynamicResource BtnGhost}" Width="130"
                            Click="BtnCheckUrls_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnSearch" Content="🔍  ISO suchen"
                            Style="{DynamicResource BtnGhost}" Width="130"
                            Click="BtnSearch_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnEditDb" Content="🗃  Datenbank"
                            Style="{DynamicResource BtnGhost}" Width="130"
                            Click="BtnEditDb_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnHealthCheck" Content="🩺  DB-Gesundheitscheck"
                            Style="{DynamicResource BtnGhost}" Width="170"
                            ToolTip="Prüft für alle Distros in der Datenbank, ob sie aktuell online erreichbar und ladbar sind."
                            Click="BtnHealthCheck_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnCopyUsb" Content="🔁  Verpasste Kopien nachholen"
                            Style="{DynamicResource BtnGhost}" Width="210"
                            ToolTip="Manuelles Sicherheitsnetz: kopiert bereits lokal vollständig heruntergeladene, ausgewählte ISOs (erneut) auf den Stick — z.B. wenn die automatische 'Jetzt kopieren?'-Nachfrage abgelehnt wurde oder eine vorherige Kopie fehlgeschlagen ist. Der automatische Scan bietet dieselbe ISO pro Stick nur einmal je Sitzung an."
                            Click="BtnCopyUsb_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnVerifyIntegrity" Content="🔒  Integrität prüfen"
                            Style="{DynamicResource BtnGhost}" Width="160"
                            ToolTip="Prüft die ISOs auf dem gewählten Stick gegen den beim Download/Import gespeicherten SHA-256-Referenzhash."
                            Click="BtnVerifyIntegrity_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnGitHubToken" Content="🔑  GitHub-Token"
                            Style="{DynamicResource BtnGhost}" Width="130"
                            ToolTip="Optional: hebt das API-Limit für GitHub-basierte Distros von 60 auf 5000 Anfragen/Std an."
                            Click="BtnGitHubToken_Click"/>
                </StackPanel>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ProgressBar Grid.Column="0" x:Name="ProgressBar"
                                 Height="5" Maximum="100" Value="{Binding ProgressPercent, Mode=OneWay}"
                                 VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" x:Name="FooterLbl"
                               Foreground="{DynamicResource BrushDim}"
                               FontSize="10.5" VerticalAlignment="Center"
                               Margin="12,0,0,0"/>
                </Grid>
            </Grid>
        </Border>

        <!-- ── Statusleiste ────────────────────────────────────────────── -->
        <Border Grid.Row="4" Background="{DynamicResource BrushCard}"
                BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,1,0,0">
            <Grid Margin="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="StatusLbl"
                           Text="{Binding StatusText}"
                           VerticalAlignment="Center"
                           FontSize="11" Foreground="{DynamicResource BrushMid}"/>
                <CheckBox Grid.Column="1" x:Name="ChkShowInfo"
                          Content="Info-Fenster (Mouseover) anzeigen"
                          IsChecked="True" FontSize="10.5"
                          VerticalAlignment="Center"/>
            </Grid>
        </Border>

    </Grid>
</Window>
